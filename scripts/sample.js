console.log('it seems to work');
let vect = NewVector(3, 4, 0);
console.log(`vector: ${JSON.stringify(vect)}`);
console.log(`vector length = ${VLength(vect)}`);
console.log(`make vector ${JSON.stringify(MakeVector([1, 2, 3]))}`);